# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# uv lockfile — machine-local, each user resolves their own
uv.lock

# Private ideas — not for public repo
ideas/

# Obsidian workspace files
.obsidian/

# Dropbox sync config — machine-local, not committed
.dropbox-sync-target

# Test corpus cached outputs — fetched from arXiv, not committed
tests/corpus/tarballs/*.tar.gz
tests/corpus/tarballs/*.gz
tests/corpus/expected/*.md
